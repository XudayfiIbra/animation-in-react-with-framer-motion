import Transition from "../transition";
const Portfolio = () => {
  return (
    <div className="flex items-center justify-center ">
      <h1 className="mt-[4em] text-6xl">Portfolio</h1>
    </div>
  );
};

export default Transition(Portfolio);

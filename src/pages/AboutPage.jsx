import Transition from "../transition";
const AboutPage = () => {
  return (
    <div className="flex items-center justify-center ">
      <h1 className="mt-[4em] text-6xl">About</h1>
    </div>
  );
};

export default Transition(AboutPage);

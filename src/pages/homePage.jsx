import Transition from "../transition";

const HomePage = () => {
  return (
    <div className="flex items-center justify-center ">
      <h1 className="mt-[4em] text-6xl">Home</h1>
    </div>
  );
};

export default Transition(HomePage);
